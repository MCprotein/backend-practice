{
  "type": "module",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mongoose": "^8.6.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "scripts": {
    "dev": "nodemon --env-file=${npm_config_env:-.env} src/app.js",
    "start": "node --env-file=${npm_config_env:-.env} src/main.js",
    "start2": "node --env-file=${npm_config_env:-.env} src/app.js",
    "seed": "node --env-file=${npm_config_env:-.env} data/seed.js"
  }
}
